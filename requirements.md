```
# 课表大屏展示系统 - 业务需求文档

## 一、项目概述
开发课表展示网页，核心功能：
- 根据当前系统时间，自动展示距离最近的一节课
- 安卓手机竖屏优先，电脑端自适应
- 通过 Vercel Serverless Function 调用正方教务系统 API 自动拉取课表
- 支持手动调课覆盖层（存在浏览器 localStorage）
- 倒计时每秒刷新
- AI科技简约风格，支持白天/黑夜一键切换主题
- 前端部署在 GitHub Pages，后端部署在 Vercel Functions

## 二、技术架构
- 前端：纯静态 HTML/CSS/JS，部署 GitHub Pages
- 后端：Vercel Serverless Functions (Python)，调用 zfn_api 拉课表
- 学号密码：存储在 Vercel 环境变量，前端不可见
- 手动调课：存储在用户手机浏览器 localStorage，优先级高于正方接口数据

## 三、前端大屏展示页需求
### 3.1 页面布局（手机竖屏优先，全部内容一屏展示，无需滚动）
- 页面右上角：🌞/🌙 主题切换按钮，一键切换日间/夜间，带平滑过渡动画
- 页面顶部栏：当前日期、中文星期、实时时钟 HH:MM:SS（每秒更新）
- 主体卡片依次展示：课程名称、教室、上课起止时间、授课老师、倒计时/提示文案、进度条

### 3.2 字号层级（优先级从高到低）
- 倒计时数字：56~72px，超粗，页面最醒目
- 课程名称：48~64px，超粗
- 教室名称：40~52px，加粗（重点放大，快速找教室）
- 上课时间：24~28px
- 老师名字：18~20px
- 顶部日期+星期：16~18px
- 顶部实时时钟：20~24px，加粗

电脑大屏：最大宽度800px，居中展示，等比例缩放。

### 3.3 UI视觉规范｜极简赛博科技（柔和版）
- 卡片圆角：20px
- 页面背景：极淡径向渐变
- 卡片：轻微阴影 + 细边框
- 倒计时数字变化带平滑过渡动画
- 无花哨特效，信息优先，安卓阳光下可读

日间配色：
- 页面背景：#f5f7fa 极浅灰蓝径向渐变
- 卡片：纯白，边框 #e2e8f0，轻微阴影
- 待上课卡片：#2563eb 科技蓝
- 上课中卡片：#10b981 科技绿

夜间配色：
- 页面背景：#0f172a 深蓝黑径向渐变
- 卡片：#1e293b，边框 #334155
- 保留科技蓝绿，亮度提升保证可读

### 3.4 课程状态规则
| 状态 | 卡片色 | 文字 | 倒计时文案 |
| ---- | ---- | ---- | ---- |
| 待上课（间隔≤48h） | 科技蓝 | 白色 | 距离上课还有 XX 分钟 |
| 待上课（间隔>48h） | 科技蓝 | 白色 | 距离下次开课还有较长假期 |
| 上课中 | 科技绿 | 白色 | 距离下课还有 XX 分钟 |
| 已结束 | 不展示 | - | 自动找下一节 |

### 3.4.1 时间进度条（仅间隔≤48h显示）
- 宽度占卡片80%，居中，高度8~12px，圆角全圆
- 半透明白色背景，白色填充，每秒更新，平滑过渡
- 待上课：进度从满逐渐减少
- 上课中：进度从左往右逐渐填满

### 3.5 时间逻辑
状态判断：
- 当前时间 ≥ 开始时间 且 < 结束时间 → 上课中
- 当前时间 < 开始时间 → 待上课
- 当前时间 ≥ 结束时间 → 已结束，自动找下一节

找下一节课：
1. 今天剩余课程中最早的一节
2. 今天没课了 → 明天第一节
3. 周日结束 → 周一第一节

长间隔规则（>48小时）：
- 隐藏分钟倒计时，显示假期提示文案
- 不渲染进度条
- 课程基础信息正常展示

倒计时：每1秒刷新一次

### 3.6 数据合并逻辑（重要）
课程数据有两个来源，合并规则：
1. 正方接口拉取的课表作为基础数据
2. localStorage 中的手动调课覆盖层优先级更高
3. 手动覆盖可以：修改某节课的教室/时间、删除某节课、新增临时课程
4. 合并后统一交给前端展示逻辑处理

## 四、手动调课覆盖层需求
通过一个简单的设置入口（长按或隐藏入口）进入手动编辑模式：
- 可以新增临时课程（课程名、星期、开始结束时间、教室、老师）
- 可以修改正方拉来的某节课的教室/老师等字段
- 可以临时跳过某节课（标记为今天不上）
- 所有修改存在 localStorage，只影响本机显示
- 提供"清除全部手动修改"按钮

## 五、后端 API 需求（Vercel Function）
### GET /api/timetable
返回标准化课程列表，格式：
```json
{
  "courses": [
    {
      "name": "高等数学",
      "weekday": 1,
      "startTime": "08:30",
      "endTime": "10:00",
      "classroom": "实训楼203",
      "teacher": "张老师"
    }
  ],
  "updatedAt": "2026-09-24 12:00:00",
  "source": "zf_api"
}
```

后端逻辑：

1. 从 Vercel 环境变量读取学号、密码、正方系统 base_url
2. 用 zfn_api 登录正方系统
3. 拉取当前学年学期课表
4. 转换为标准化格式
5. 缓存结果（减少正方请求频率）
6. 返回给前端

## 六、验收标准

1. 首页自动展示距离当前时间最近的一节课
2. 倒计时每秒更新，带平滑过渡动画
3. 安卓竖屏一屏看完，倒计时 / 课程名 / 教室字号足够大
4. 右上角🌞/🌙切换主题，平滑过渡
5. 待上课 / 上课中（≤48h）有进度条，每秒更新
6. 课程数据优先从正方接口拉取，localStorage 手动修改优先覆盖
7. 手动调课：可新增 / 修改 / 跳过课程，存在 localStorage
8. 当日课结束后自动展示明天第一节
9. 间隔超过 48h 显示假期提示，隐藏倒计时和进度条
10. 前端部署 GitHub Pages，后端部署 Vercel，联调正常

## 七、开发提示

优先保证安卓手机竖屏体验。学号密码严禁出现在前端代码。UI 装饰克制，阳光下高对比度可读。核心逻辑必须写注释。